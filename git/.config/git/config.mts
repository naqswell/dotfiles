# Applied to any repo whose remote URL is on gitlab.services.mts.ru
# (includeIf hasconfig:remote.*.url in ~/.gitconfig — по URL, не по пути,
# поэтому работает в ~/!!MTS, ~/mts или любом каталоге).
[user]
	name = IvanovNA
	email = ivanovna@mtsbank.ru
