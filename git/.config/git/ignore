# Global gitignore — applies to every repo via core.excludesFile.

# Claude Code local permissions (per-project, never commit)
**/.claude/settings.local.json

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editors
*.swp
*.swo
*~
.vscode/
.idea/
*.iml

# Logs / OS noise
*.log

# Direnv / env files
.envrc
.env
.env.local

# Secrets — никогда не коммитить (defense-in-depth поверх per-repo .gitignore)
.zshrc.local
*.secret
*_token
id_ed25519
id_ed25519.*
id_rsa
id_rsa.*
*.pem
*.jks
auth.json
# *.key — при ложных срабатываниях добавлять нужный файл через `git add -f`
*.key
